#include <stdio.h>
/**
 *main - uses printf to print a set of strings and ends with a new line
 *Return: returns 0 when the programme runs correctly. 
 */
int main(void)
{
  printf("with proper grammar, but the outcome is a piece of art,\n");
  return (0);
}
