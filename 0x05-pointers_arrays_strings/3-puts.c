#include "main.h"
/**
 * _puts - prints a string and a new line
 * @str: pointer to the string to print
 *Return: void
 */
void _puts(char *str)
{
int i;
int j;
for (i = 0; i < str[j]; i++)
{
_putchar(str[j]);
}
_putchar('\n');
}
