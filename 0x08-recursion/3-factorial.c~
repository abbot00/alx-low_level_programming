/**
 * factorial - a function that returns the factorial of a given
 *  number usig recursion.
 *  @n: an input number
 *  Return: a number.
 */
int factorial(int n)
{
if (n > 0)
{
if (n < 0)
return (-1);
if (n == 1)
return (1);
else
return (n * factorial(n - 1));
}
else
return (0);
}
