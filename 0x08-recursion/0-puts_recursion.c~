#include "main.h"

/**
 * _puts_recursion - function that prints a string follow
 * by a new line.
 * @s: Pointer to input string
 * Return: void
 */
void _puts_recursion(char *s)
{
if (*s == '\0')
{
_putchar('\n');
return;
}
_putchar(*s);
_puts_recurshion(s + 1);
}
